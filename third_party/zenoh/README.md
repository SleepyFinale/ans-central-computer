# Zenoh standalone binaries (not committed)

Run from the workspace root:

```bash
./scripts/comms/install_zenoh_bridge.sh
```

That downloads a musl `zenoh-bridge-ros2dds` compatible with Ubuntu 22.04 into this directory.
